nws[is.nan(nws$V1),]
nwsd[is.nan(nwsd$V1),]
# remove NAs for final evaluation
nt<-na.omit(nt)
nwd<-na.omit(nwd)
nws<-na.omit(nws)
nwsd<-na.omit(nwsd)
#  find those activities that are similar to neanderthals
nt[nt$V2>.05,]
nwd[nwd$V2>.05,]
nws[nt$V2>.05,]
nwsd[nwd$V2>.05,]
setwd(dir='Desktop/Spring 2016/Neander_Trauma/')
library(plyr)
# read in the data
df <- read.csv('Data/NEISS/sport_category_final.csv')
# create a contigency table
orig_table <- table(df$prod1,df$body_part)
# create a datafame from the contigency table (at least one that is intuitive)
tm<-as.data.frame.matrix(orig_table) # tm stands for table matrix
# create new columns based on body part combinations
head_neck <- tm$face + tm$neck + tm$head
shoulder_arm <- tm$`upper arm` + tm$`lower arm` + tm$shoulder + tm$elbow
hand <- tm$hand + tm$finger + tm$wrist
pelvis <- tm$`pubic region` + tm$hip # look up Berger femoral neck categorization and within the data
leg <- tm$knee + tm$`lower leg` + tm$`upper leg`
foot <- tm$foot + tm$toe + tm$ankle
trunk <- tm$`upper trunk` + tm$back
# combine columns into a new dataframe
final<-as.data.frame(cbind(head_neck,shoulder_arm,hand,pelvis,leg,foot,trunk))
rownames(final)<-rownames(tm) # index will be the activity name
# recreate data from Berger Trinkaus papers
neander_tot<-c(8,4,7,1,1,3,3)
nea_wo_djd<-c(7,4,7,1,0,3,1)
nea_wo_shan1<-c(6,4,5,1,1,3,1)
nea_wo_shan1_djd<-c(5,4,5,1,0,3,0)
# apply chi square for neander total and every sport
nt<-t(apply(final,1,function(x) {
new<- cbind(neander_tot,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nwd<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_djd,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nws<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_shan1,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nwsd<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_shan1_djd,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
# change tables above into data frames
nt<-as.data.frame(nt,row.names = rownames(nt))
nwd<-as.data.frame(nwd,row.names=rownames(nwd))
nws<-as.data.frame(nws,row.names=rownames(nws))
nwsd<-as.data.frame(nwsd,row.names = rownames(nwsd))
# find those rows activities resulting in NaNs or the expected value within the chi square test is nothing
nt[is.nan(nt$V1),]
nwd[is.nan(nwd$V1),]
nws[is.nan(nws$V1),]
nwsd[is.nan(nwsd$V1),]
# remove NAs for final evaluation
nt<-na.omit(nt)
nwd<-na.omit(nwd)
nws<-na.omit(nws)
nwsd<-na.omit(nwsd)
#  find those activities that are similar to neanderthals
nt[nt$V2>.05,]
nwd[nwd$V2>.05,]
nws[nt$V2>.05,]
nwsd[nwd$V2>.05,]
nt<-as.data.frame(nt,row.names = rownames(nt),colnames(c('X2','P-Value')))
nt
nt<-as.data.frame(nt,row.names = rownames(nt),colnames = c('X2','P-Value')))
nt<-as.data.frame(nt,row.names = rownames(nt),colnames = c('X2','P-Value'))
nt
nt<-as.data.frame(nt,row.names = rownames(nt),names = c('X2','P-Value'))
nt
nt<-as.data.frame(nt,row.names = rownames(nt), col.names = c('x2','P-Value'))
nt
names(nt)
names(nt)<- c('X2','P-Value')
names(nt)
nt[is.nan(nt$V1),]
nwd[is.nan(nwd$V1),]
nws[is.nan(nws$V1),]
nwsd[is.nan(nwsd$V1),]
names(nt)<- c('X2','P-Value')
names(nwd)<- c('X2','P-Value')
names(nws)<- c('X2','P-Value')
names(nwsd)<- c('X2','P-Value')
nt<-na.omit(nt)
nwd<-na.omit(nwd)
nws<-na.omit(nws)
nwsd<-na.omit(nwsd)
#  find those activities that are similar to neanderthals
nt[nt$V2>.05,]
nwd[nwd$V2>.05,]
nws[nt$V2>.05,]
nwsd[nwd$V2>.05,]
setwd(dir='Desktop/Spring 2016/Neander_Trauma/')
library(plyr)
# read in the data
df <- read.csv('Data/NEISS/sport_category_final.csv')
# create a contigency table
orig_table <- table(df$prod1,df$body_part)
# create a datafame from the contigency table (at least one that is intuitive)
tm<-as.data.frame.matrix(orig_table) # tm stands for table matrix
# create new columns based on body part combinations
head_neck <- tm$face + tm$neck + tm$head
shoulder_arm <- tm$`upper arm` + tm$`lower arm` + tm$shoulder + tm$elbow
hand <- tm$hand + tm$finger + tm$wrist
pelvis <- tm$`pubic region` + tm$hip # look up Berger femoral neck categorization and within the data
leg <- tm$knee + tm$`lower leg` + tm$`upper leg`
foot <- tm$foot + tm$toe + tm$ankle
trunk <- tm$`upper trunk` + tm$back
# combine columns into a new dataframe
final<-as.data.frame(cbind(head_neck,shoulder_arm,hand,pelvis,leg,foot,trunk))
rownames(final)<-rownames(tm) # index will be the activity name
# recreate data from Berger Trinkaus papers
neander_tot<-c(8,4,7,1,1,3,3)
nea_wo_djd<-c(7,4,7,1,0,3,1)
nea_wo_shan1<-c(6,4,5,1,1,3,1)
nea_wo_shan1_djd<-c(5,4,5,1,0,3,0)
# apply chi square for neander total and every sport
nt<-t(apply(final,1,function(x) {
new<- cbind(neander_tot,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nwd<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_djd,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nws<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_shan1,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nwsd<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_shan1_djd,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
# change tables above into data frames
nt<-as.data.frame(nt,row.names = rownames(nt))
nwd<-as.data.frame(nwd,row.names=rownames(nwd))
nws<-as.data.frame(nws,row.names=rownames(nws))
nwsd<-as.data.frame(nwsd,row.names = rownames(nwsd))
# find those rows activities resulting in NaNs or the expected value within the chi square test is nothing
nt[is.nan(nt$V1),]
nwd[is.nan(nwd$V1),]
nws[is.nan(nws$V1),]
nwsd[is.nan(nwsd$V1),]
names(nt)<- c('X2','P-Value')
names(nwd)<- c('X2','P-Value')
names(nws)<- c('X2','P-Value')
names(nwsd)<- c('X2','P-Value')
# remove NAs for final evaluation
nt<-na.omit(nt)
nwd<-na.omit(nwd)
nws<-na.omit(nws)
nwsd<-na.omit(nwsd)
#  find those activities that are similar to neanderthals
nt[nt$V2>.05,]
nwd[nwd$V2>.05,]
nws[nt$V2>.05,]
nwsd[nwd$V2>.05,]
setwd(dir='Desktop/Spring 2016/Neander_Trauma/')
library(plyr)
# read in the data
df <- read.csv('Data/NEISS/sport_category_final.csv')
# create a contigency table
orig_table <- table(df$prod1,df$body_part)
# create a datafame from the contigency table (at least one that is intuitive)
tm<-as.data.frame.matrix(orig_table) # tm stands for table matrix
# create new columns based on body part combinations
head_neck <- tm$face + tm$neck + tm$head
shoulder_arm <- tm$`upper arm` + tm$`lower arm` + tm$shoulder + tm$elbow
hand <- tm$hand + tm$finger + tm$wrist
pelvis <- tm$`pubic region` + tm$hip # look up Berger femoral neck categorization and within the data
leg <- tm$knee + tm$`lower leg` + tm$`upper leg`
foot <- tm$foot + tm$toe + tm$ankle
trunk <- tm$`upper trunk` + tm$back
# combine columns into a new dataframe
final<-as.data.frame(cbind(head_neck,shoulder_arm,hand,pelvis,leg,foot,trunk))
rownames(final)<-rownames(tm) # index will be the activity name
# recreate data from Berger Trinkaus papers
neander_tot<-c(8,4,7,1,1,3,3)
nea_wo_djd<-c(7,4,7,1,0,3,1)
nea_wo_shan1<-c(6,4,5,1,1,3,1)
nea_wo_shan1_djd<-c(5,4,5,1,0,3,0)
# apply chi square for neander total and every sport
nt<-t(apply(final,1,function(x) {
new<- cbind(neander_tot,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nwd<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_djd,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nws<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_shan1,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nwsd<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_shan1_djd,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
# change tables above into data frames
nt<-as.data.frame(nt,row.names = rownames(nt))
nwd<-as.data.frame(nwd,row.names=rownames(nwd))
nws<-as.data.frame(nws,row.names=rownames(nws))
nwsd<-as.data.frame(nwsd,row.names = rownames(nwsd))
# find those rows activities resulting in NaNs or the expected value within the chi square test is nothing
nt[is.nan(nt$V1),]
nwd[is.nan(nwd$V1),]
nws[is.nan(nws$V1),]
nwsd[is.nan(nwsd$V1),]
# remove NAs for final evaluation
nt<-na.omit(nt)
nwd<-na.omit(nwd)
nws<-na.omit(nws)
nwsd<-na.omit(nwsd)
nt[nt$V2>.05,]
names(nt)<- c('X2','P-Value')
names(nwd)<- c('X2','P-Value')
names(nws)<- c('X2','P-Value')
names(nwsd)<- c('X2','P-Value')
nt[nt$V2>.05,]
nwd[nwd$V2>.05,]
nws[nt$V2>.05,]
nwsd[nwd$V2>.05,]
names(nt)
nt
nt[nt$`P-Value`>.05,]
nwd[nwd$`P-Value`>.05,]
nws[nt$`P-Value`>.05,]
nwsd[nwd$`P-Value`>.05,]
nwsd
chi2summarize<-function(df){
new<-as.data.frame(df,row.names = rownames(df))
names(new)<-c('X2','P-Value')
rowsna<-new[is.nan(new$'X2'),]
final<-na.omit(new)
final[final$`P-Value`>.05,]
}
nt<-t(apply(final,1,function(x) {
new<- cbind(neander_tot,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
chi2summarize(nt)
first<-chi2summarize(nt)
first
first$final
first.final
?setClass
track <- setClass("track",
slots = c(x="numeric", y="numeric"))
track(x=2,y =3)
trackCurve <- setClass("trackCurve",
slots = c(smooth = "numeric"),
contains = "track")
trackCurve
trackCurve()
chi2summarize<-function(df){
new<-as.data.frame(df,row.names = rownames(df))
names(new)<-c('X2','P-Value')
g<-list(
rowsna<-new[is.nan(new$'X2'),],
final<-na.omit(new),
)
}
chi2summarize(nt)
s<-function(df){
frame<-as.data.frame(df,row.names = rownames(df))
names(frame) = c('X2','P-Value')
c<- list(
rowna=frame[is.nan(frame$X2),],
final=na.omit(frame)
)
return(c)
}
s(nt)
nt$final
f<-s(nt)
f$final
f$rowna
f<-s(nt)
f$final[f$final$`P-Value`>.05,]
chi2cleanup<-function(table){
# read in one of the chi square tables (nt, nwd, nws or nwsd)
frame<-as.data.frame(df,row.names = rownames(table))
names(frame) = c('X2','P-Value')
# create two callable objects, 1) the rows that have NAs and 2) the final data frame for some final manipulation
c<- list(
rowna=frame[is.nan(frame$X2),],
final=na.omit(frame)
nodif=final[final$X2>.05,]
)
return(c)
}
chi2cleanup<-function(table){
# read in one of the chi square tables (nt, nwd, nws or nwsd)
frame<-as.data.frame(df,row.names = rownames(table))
names(frame) = c('X2','P-Value')
# create two callable objects, 1) the rows that have NAs and 2) the final data frame for some final manipulation
c<- list(
rowna=frame[is.nan(frame$X2),],
final=na.omit(frame),
nodif=final[final$X2>.05,]
)
return(c)
}
f<-s(nt)
f$nodif
chi2cleanup<-function(table){
# read in one of the chi square tables (nt, nwd, nws or nwsd)
frame<-as.data.frame(df,row.names = rownames(table))
names(frame) = c('X2','P-Value')
# create two callable objects, 1) the rows that have NAs and 2) the final data frame for some final manipulation
c<- list(
rowna=frame[is.nan(frame$X2),],
final=na.omit(frame),
nodif=final[final$`P-Value`>.05,]
)
return(c)
}
f<-s(nt)
f$nodif
chi2cleanup<-function(table){
# read in one of the chi square tables (nt, nwd, nws or nwsd)
frame<-as.data.frame(df,row.names = rownames(table))
names(frame) = c('X2','P-Value')
fin<-na.omit(frame)
# create two callable objects, 1) the rows that have NAs and 2) the final data frame for some final manipulation
c<- list(
rowna=frame[is.nan(frame$X2),],
final=fin,
nodif=fin[fin$`P-Value`>.05,]
)
return(c)
}
f$nodif
f<-s(nt)
f$nodif
chi2cleanup<-function(table){
# read in one of the chi square tables (nt, nwd, nws or nwsd)
frame<-as.data.frame(df,row.names = rownames(table))
names(frame) = c('X2','P-Value')
# create two callable objects, 1) the rows that have NAs and 2) the final data frame for some final manipulation
c<- list(
rowna=frame[is.nan(frame$X2),],
final=na.omit(frame)
)
return(c)
}
f<-s(nt)
f$rowna
f$final
nwsd<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_shan1_djd,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
chi2cleanup<-function(table){
# read in one of the chi square tables (nt, nwd, nws or nwsd)
frame<-as.data.frame(df,row.names = rownames(table))
names(frame) = c('X2','P-Value')
# create two callable objects, 1) the rows that have NAs and 2) the final data frame for some final manipulation
c<- list(
rowna=frame[is.nan(frame$X2),],
final=na.omit(frame),
nodif=final[final$`P-Value`>0.05,]
)
return(c)
}
f<-chi2cleanup(nf)
nt<-t(apply(final,1,function(x) {
new<- cbind(neander_tot,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
f<-chi2cleanup(nf)
f<-chi2cleanup(nt)
chi2cleanup<-function(table){
# read in one of the chi square tables (nt, nwd, nws or nwsd)
frame<-as.data.frame(df,row.names = rownames(table))
names(frame) = c('X2','P-Value')
# create two callable objects, 1) the rows that have NAs and 2) the final data frame for some final manipulation
c<- list(
rowna=frame[is.nan(frame$X2),],
final=na.omit(frame)
)
return(c)
}
chi2cleanup(nt)
chi2cleanup<-function(table){
# read in one of the chi square tables (nt, nwd, nws or nwsd)
frame<-as.data.frame(table,row.names = rownames(table))
names(frame) = c('X2','P-Value')
# create two callable objects, 1) the rows that have NAs and 2) the final data frame for some final manipulation
c<- list(
rowna=frame[is.nan(frame$X2),],
final=na.omit(frame)
)
return(c)
}
f<-chi2cleanup(nt)
f$rowna
f$final
chi2cleanup<-function(table){
# read in one of the chi square tables (nt, nwd, nws or nwsd)
frame<-as.data.frame(table,row.names = rownames(table))
names(frame) = c('X2','P-Value')
# create two callable objects, 1) the rows that have NAs and 2) the final data frame for some final manipulation
c<- list(
rowna=frame[is.nan(frame$X2),],
final=na.omit(frame),
nodif=final[final$`P-Value`>.05,]
)
return(c)
}
f<-chi2cleanup(nt)
f$nodif
nt<-t(apply(final,1,function(x) {
new<- cbind(neander_tot,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nwd<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_djd,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nws<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_shan1,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nwsd<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_shan1_djd,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
first<-chi2cleanup(nt)
secon<-chi2cleanup(nwd)
second<-chi2cleanup(nwd)
third<-chi2cleanup(nws)
fourth<-chi2cleanup(nwsd)
fourth$final
fourth$rowna
first$final[first$final$`P-Value`>.05,]
second$final[second$final$`P-Value`>.05,]
third$final[third$final$`P-Value`>.05,]
fourth$final[fourth$final$`P-Value`>.05,]
setwd(dir='Desktop/Spring 2016/Neander_Trauma/')
library(plyr)
# read in the data
df <- read.csv('Data/NEISS/sport_category_final.csv')
# create a contigency table
orig_table <- table(df$prod1,df$body_part)
# create a datafame from the contigency table (at least one that is intuitive)
tm<-as.data.frame.matrix(orig_table) # tm stands for table matrix
# create new columns based on body part combinations
head_neck <- tm$face + tm$neck + tm$head
shoulder_arm <- tm$`upper arm` + tm$`lower arm` + tm$shoulder + tm$elbow
hand <- tm$hand + tm$finger + tm$wrist
pelvis <- tm$`pubic region` + tm$hip # look up Berger femoral neck categorization and within the data
leg <- tm$knee + tm$`lower leg` + tm$`upper leg`
foot <- tm$foot + tm$toe + tm$ankle
trunk <- tm$`upper trunk` + tm$back
# combine columns into a new dataframe
final<-as.data.frame(cbind(head_neck,shoulder_arm,hand,pelvis,leg,foot,trunk))
rownames(final)<-rownames(tm) # index will be the activity name
# recreate data from Berger Trinkaus papers
neander_tot<-c(8,4,7,1,1,3,3)
nea_wo_djd<-c(7,4,7,1,0,3,1)
nea_wo_shan1<-c(6,4,5,1,1,3,1)
nea_wo_shan1_djd<-c(5,4,5,1,0,3,0)
# apply chi square for neander total and every sport
nt<-t(apply(final,1,function(x) {
new<- cbind(neander_tot,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nwd<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_djd,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nws<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_shan1,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
nwsd<-t(apply(final,1,function(x) {
new<- cbind(nea_wo_shan1_djd,x)
ch <- chisq.test(new)
c(unname(ch$statistic), ch$p.value)}))
# create a function to cleanup the chi square tables and be able to pull out different objects from this cleanup
chi2cleanup<-function(table){
# read in one of the chi square tables (nt, nwd, nws or nwsd)
frame<-as.data.frame(table,row.names = rownames(table))
names(frame) = c('X2','P-Value')
# create two callable objects, 1) the rows that have NAs and 2) the final data frame for some final manipulation
c<- list(
rowna=frame[is.nan(frame$X2),],
final=na.omit(frame)
)
return(c)
}
# name cleaned up chi square tables
first<-chi2cleanup(nt)
second<-chi2cleanup(nwd)
third<-chi2cleanup(nws)
fourth<-chi2cleanup(nwsd)
# find those that have are similar to neanderthal samples (rename these!!!!)
first$final[first$final$`P-Value`>.05,] # for total neanderthal sample
second$final[second$final$`P-Value`>.05,] # neanderthal without djd
third$final[third$final$`P-Value`>.05,] # neanderthal without shan
fourth$final[fourth$final$`P-Value`>.05,] # neanderthal without djd or shan
